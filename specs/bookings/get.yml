definitions:
  Booking:
    type: object
    properties:
      start_time:
        type: string
      end_time:
        type: string
responses:
  200:
    description: A list of bookings
    content:
      application/json:
        schema:
          $ref: '#/definitions/Booking'

